[package]
name = "tauri-plugin-background"
version = "0.1.0"
description = "Keeps the app's process alive on Android while AI work is in flight"
edition = "2021"
links = "tauri-plugin-background"

[dependencies]
tauri = { version = "2" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
  'cfg(mobile)',
  'cfg(desktop)',
] }
